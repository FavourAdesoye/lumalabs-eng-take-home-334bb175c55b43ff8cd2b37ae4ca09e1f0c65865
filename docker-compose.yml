services:
  app:
    build: .
    ports:
      - "${HOST_PORT:-3001}:3001"
    environment:
      PORT: "3001"
      CLIENT_ORIGIN: "http://localhost:${HOST_PORT:-3001}"
      SEMANTIC_SEARCH_ENABLED: ${SEMANTIC_SEARCH_ENABLED:-true}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
